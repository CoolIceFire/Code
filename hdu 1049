#include<stdio.h>
int main()
{
    int n,u,d,i,h;
    while(~scanf("%d%d%d",&n,&u,&d)&&n)
    {
        for(h=i=0;;i++)
        {
            if(i%2==0)h+=u;
            else h-=d;
            if(h>=n)break;
        }
        printf("%d\n",i+1);
    }
    return 0;
}
