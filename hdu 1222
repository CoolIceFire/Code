# include <stdio.h>


int gcd(int a, int b){return a%b?gcd(b,a%b):b;}


int main ()
{
    int T, n, m ;
    scanf ("%d", &T) ;
    while (T--)
    {
        scanf ("%d%d", &n, &m) ;
        puts (gcd(n,m) == 1 ? "NO" : "YES") ;
    }
}
