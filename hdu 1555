#include<stdio.h>

int main()
{
    int m,k,s,i;
    while(~scanf("%d%d",&m,&k)&&(m||k))
    {
        s=0;
        for(i=1;i<=m;i++)
        {
            if(i%k==0)
                m++;
            s++;
        }
        printf("%d\n",s);
    }
}
