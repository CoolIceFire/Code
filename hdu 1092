#include<stdio.h>
int main()
{
    int a,b,s;
    while(scanf("%d",&a)!=EOF)
    {
        s=0;
        if(a)
        {
            while(a--)
                scanf("%d",&b),s+=b;
            printf("%d\n",s);
        }
    }
}
