# include <stdio.h>
int main()
{
    int m,n,i,j;
    scanf("%d",&n);
    while(n--)
    {
        scanf("%d",&m);
        for(i=0;i<3*m;i++)
        {
            for(j=0;j<m;j++)
                printf("HDU");
            puts("");
        }
    }
    return 0;
}
